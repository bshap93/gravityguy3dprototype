using UnityEngine;
using UnityEngine.UI;

namespace HUD
{
    public class ThrusterTextController : MonoBehaviour
    {
        public GameObject player;
        // Start is called before the first frame update
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {



        }
    }
}
