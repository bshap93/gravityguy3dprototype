using UnityEngine;

public class DockingSystem : MonoBehaviour
{
    public GameObject dockingTarget;
    public GameObject dockingCraft;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
